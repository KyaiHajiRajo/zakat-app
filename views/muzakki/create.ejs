<%- include('../layouts/header', { title: 'Tambah Muzakki - Zakat Fitrah App' , bodyClass: 'bg-gray-50' ,
  currentPage: 'muzakki' }) %>

  <div class="page-wrapper">
    <!-- Modern Header -->
    <div class="page-header">
      <div class="header-content-wrapper">
        <div class="header-left-section">
          <a href="/muzakki" class="back-button">
            <i class="fas fa-arrow-left"></i>
          </a>
          <div class="header-icon-box">
            <img src="/images/logo-dkm.png" alt="Logo DKM" class="header-logo">
          </div>
          <div class="header-text-box">
            <h1 class="header-title-text">Tambah Muzakki</h1>
            <p class="header-subtitle-text">Daftarkan muzakki baru dan hitung zakat fitrah</p>
          </div>
        </div>
        <div class="header-actions">
          <div class="info-badge">
            <i class="fas fa-info-circle"></i>
            <span>Form Pendaftaran</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Form Container -->
    <div class="form-container">
      <form action="/muzakki" method="POST" id="muzakkiForm">
        <!-- RT Selection Card -->
        <div class="form-card">
          <div class="card-header">
            <div class="card-header-icon">
              <i class="fas fa-map-marker-alt"></i>
            </div>
            <div class="card-header-text">
              <h2 class="card-title">Pilih RT</h2>
              <p class="card-subtitle">Tentukan RT tempat muzakki terdaftar</p>
            </div>
          </div>
          <div class="card-body">
            <input
              type="hidden"
              id="tanggal"
              name="tanggal"
              value="<%= defaultTanggal %>"
            />
            <div class="form-group">
              <label for="rt_id" class="form-label">
                <i class="fas fa-home mr-1"></i>RT <span class="required">*</span>
              </label>
              <select id="rt_id" name="rt_id" required class="form-select">
                <option value="">Pilih RT</option>
                <% rtList.forEach(function(rt) { %>
                  <option value="<%= rt.id %>">
                    RT <%= rt.nomor_rt %> - <%= rt.ketua_rt %>
                  </option>
                  <% }); %>
              </select>
            </div>
          </div>
        </div>

        <!-- Data Identitas Muzakki Card -->
        <div class="form-card">
          <div class="card-header">
            <div class="card-header-icon">
              <i class="fas fa-id-card"></i>
            </div>
            <div class="card-header-text">
              <h2 class="card-title">Data Identitas Muzakki</h2>
              <p class="card-subtitle">Masukkan data lengkap muzakki yang akan didaftarkan</p>
            </div>
          </div>
          <div class="card-body">
            <!-- Container untuk semua section muzakki -->
            <div id="muzakkiContainer">
              <!-- Section Muzakki Pertama -->
              <div class="muzakki-section" data-index="0">
                <div class="muzakki-section-header">
                  <h4 class="muzakki-section-title">
                    <i class="fas fa-user-circle"></i>Muzakki #1
                  </h4>
                </div>

                <div class="form-grid">
                  <!-- Nama Muzakki -->
                  <div class="form-group form-group-full">
                    <label for="nama_0" class="form-label">
                      <i class="fas fa-user mr-1"></i>Nama Lengkap Muzakki <span class="required">*</span>
                    </label>
                    <input type="text" id="nama_0" name="muzakki[0][nama]" required class="form-input"
                      placeholder="Masukkan nama lengkap muzakki" />
                  </div>

                  <!-- Bin/Binti -->
                  <div class="form-group">
                    <label for="bin_binti_0" class="form-label">
                      <i class="fas fa-venus-mars mr-1"></i>Bin/Binti
                    </label>
                    <select id="bin_binti_0" name="muzakki[0][bin_binti]" class="form-select">
                      <option value="">Pilih</option>
                      <option value="bin">Bin (Anak Laki-laki)</option>
                      <option value="binti">Binti (Anak Perempuan)</option>
                    </select>
                    <p class="form-hint">
                      Bin untuk laki-laki, Binti untuk perempuan
                    </p>
                  </div>

                  <!-- Nama Orang Tua -->
                  <div class="form-group form-group-double">
                    <label for="nama_orang_tua_0" class="form-label">
                      <i class="fas fa-users mr-1"></i>Nama Orang Tua
                    </label>
                    <input type="text" id="nama_orang_tua_0" name="muzakki[0][nama_orang_tua]" class="form-input"
                      placeholder="Masukkan nama ayah atau ibu sesuai bin/binti" />
                    <p class="form-hint">
                      Nama ayah jika "bin", nama ibu jika "binti"
                    </p>
                  </div>
                </div>
              </div>
            </div>

            <!-- Tombol Tambah Muzakki -->
            <button type="button" id="addMuzakkiBtn" class="btn-add-muzakki" onclick="addMuzakkiSection()">
              <i class="fas fa-plus"></i>
              <span>Tambah Muzakki</span>
            </button>
          </div>
        </div>

        <!-- Perhitungan Zakat Card -->
        <div class="form-card">
          <div class="card-header">
            <div class="card-header-icon">
              <i class="fas fa-calculator"></i>
            </div>
            <div class="card-header-text">
              <h2 class="card-title">Perhitungan Zakat</h2>
              <p class="card-subtitle">Tentukan jenis dan jumlah zakat yang dibayarkan</p>
            </div>
          </div>
          <div class="card-body">
            <div class="form-grid">
              <!-- Jumlah Jiwa -->
              <div class="form-group">
                <label for="jumlah_jiwa" class="form-label">
                  <i class="fas fa-users mr-1"></i>Jumlah Jiwa <span class="required">*</span>
                </label>
                <input type="number" id="jumlah_jiwa" name="jumlah_jiwa" required min="0"
                  class="form-input input-readonly" placeholder="0" readonly
                  title="Otomatis terisi sesuai jumlah muzakki" />
                <p class="form-hint">
                  Jumlah jiwa akan otomatis terisi sesuai jumlah muzakki yang diinput
                </p>
              </div>

              <!-- Jenis Zakat -->
              <div class="form-group">
                <label for="master_zakat_id" class="form-label">
                  <i class="fas fa-hand-holding mr-1"></i>Jenis Zakat <span class="required">*</span>
                </label>
                <select id="master_zakat_id" name="master_zakat_id" required class="form-select">
                  <option value="">Pilih jenis zakat</option>
                  <% masterZakatList.forEach(function(zakat) { %>
                    <option value="<%= zakat.id %>" data-harga="<%= zakat.harga %>" data-kg="<%= zakat.kg %>">
                      <%= zakat.nama %> 
                      <% if (parseFloat(zakat.kg) > 0) { %>
                        (<%= zakat.kg %> kg/jiwa)
                      <% } else { %>
                        (Rp <%= Math.floor(parseFloat(zakat.harga)).toLocaleString('id-ID') %>/jiwa)
                      <% } %>
                    </option>
                  <% }); %>
                </select>
              </div>

              <!-- Preview Perhitungan Zakat -->
              <div class="form-group form-group-full">
                <div class="preview-container">
                  <!-- Preview Jumlah Uang -->
                  <div class="preview-box preview-box-blue">
                    <div class="preview-icon">
                      <i class="fas fa-money-bill-wave"></i>
                    </div>
                    <div class="preview-content">
                      <div class="preview-label">Total Zakat (Uang)</div>
                      <div class="preview-value" id="preview_jumlah_uang">Rp 0</div>
                      <input type="hidden" id="jumlah_uang" name="jumlah_uang" value="0" />
                    </div>
                  </div>

                  <!-- Preview Jumlah Beras -->
                  <div class="preview-box preview-box-green">
                    <div class="preview-icon">
                      <i class="fas fa-seedling"></i>
                    </div>
                    <div class="preview-content">
                      <div class="preview-label">Total Zakat (Beras)</div>
                      <div class="preview-value" id="preview_jumlah_beras_kg">0 kg</div>
                      <input type="hidden" id="jumlah_beras_kg" name="jumlah_beras_kg" value="0" />
                    </div>
                  </div>
                </div>
              </div>

              <!-- Jumlah Bayar -->
              <div class="form-group form-group-full">
                <label for="jumlah_bayar_display" class="form-label">
                  <i class="fas fa-money-bill-wave mr-1"></i>Jumlah Bayar (Rp) <span class="required">*</span>
                </label>
                <div class="input-with-prefix">
                  <span class="input-prefix">Rp</span>
                  <input type="text" id="jumlah_bayar_display" class="form-input input-with-prefix-field"
                    placeholder="0" oninput="handleRupiahInput(this)" onblur="finalizeRupiahInput(this)"
                    onfocus="prepareRupiahInput(this)" />
                  <input type="hidden" id="jumlah_bayar" name="jumlah_bayar" required />
                </div>
                <p class="form-hint">
                  Jika lebih dari kewajiban, selisihnya akan menjadi kembalian yang bisa disedekahkan
                </p>
              </div>

              <!-- Catatan -->
              <div class="form-group form-group-full">
                <label for="catatan" class="form-label">
                  <i class="fas fa-sticky-note mr-1"></i>Catatan (Opsional)
                </label>
                <textarea id="catatan" name="catatan" rows="3" class="form-textarea"
                  placeholder="Catatan tambahan atau keterangan khusus"></textarea>
              </div>
            </div>
          </div>
        </div>

        <!-- Submit Buttons -->
        <div class="form-actions">
          <a href="/muzakki" class="btn-secondary">
            <i class="fas fa-times"></i>
            <span>Batal</span>
          </a>
          <button type="submit" class="btn-primary">
            <i class="fas fa-save"></i>
            <span>Simpan Data</span>
          </button>
        </div>
      </form>
    </div>

    <!-- Helper Information -->
    <div class="info-grid">
      <div class="info-card info-card-green">
        <div class="info-card-icon">
          <i class="fas fa-seedling"></i>
        </div>
        <div class="info-card-content">
          <h3 class="info-card-title">Zakat Beras</h3>
          <ul class="info-card-list">
            <li>Kewajiban: 2.5 kg per jiwa</li>
            <li>Dapat dibayar dengan beras atau uang senilai</li>
            <li>Lebih mudah untuk distribusi</li>
          </ul>
        </div>
      </div>

      <div class="info-card info-card-blue">
        <div class="info-card-icon">
          <i class="fas fa-coins"></i>
        </div>
        <div class="info-card-content">
          <h3 class="info-card-title">Zakat Uang</h3>
          <ul class="info-card-list">
            <li>Kewajiban: Rp 45.000 per jiwa</li>
            <li>Lebih fleksibel untuk pengelolaan</li>
            <li>Mudah untuk perhitungan dan distribusi</li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <!-- Modern Styles -->
  <style>
    .page-wrapper {
      --mz-primary: #15803d;
      --mz-primary-soft: #22c55e;
      max-width: 100%;
      width: 100%;
      margin: 0 auto;
      padding: 0.62rem 0.22rem 1.35rem;
      display: flex;
      flex-direction: column;
      gap: 0.88rem;
      position: relative;
      min-width: 0;
    }
    .page-wrapper::before,
    .page-wrapper::after {
      content: "";
      position: absolute;
      border-radius: 9999px;
      pointer-events: none;
      filter: blur(10px);
      z-index: 0;
    }
    .page-wrapper::before {
      width: 220px;
      height: 220px;
      top: 18px;
      right: -68px;
      background: radial-gradient(circle at center, rgba(34, 197, 94, 0.2), rgba(34, 197, 94, 0));
    }
    .page-wrapper::after {
      width: 170px;
      height: 170px;
      bottom: 80px;
      left: -46px;
      background: radial-gradient(circle at center, rgba(59, 130, 246, 0.15), rgba(59, 130, 246, 0));
    }
    .page-wrapper > * {
      position: relative;
      z-index: 1;
      min-width: 0;
    }
    .page-wrapper .page-header {
      background: linear-gradient(122deg, #0f766e 0%, #16a34a 58%, #22c55e 100%);
      border-radius: 18px;
      padding: 1rem 1.02rem;
      box-shadow: 0 16px 34px rgba(22, 163, 74, 0.25);
      position: relative;
      overflow: hidden;
    }
    .page-wrapper .page-header::before {
      content: "";
      position: absolute;
      width: 280px;
      height: 280px;
      top: -146px;
      right: -76px;
      border-radius: 9999px;
      background: radial-gradient(circle, rgba(255, 255, 255, 0.22) 0%, rgba(255, 255, 255, 0) 70%);
    }
    .header-content-wrapper {
      position: relative;
      z-index: 1;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 0.88rem;
      flex-wrap: wrap;
    }
    .header-left-section {
      display: flex;
      align-items: center;
      gap: 0.74rem;
      min-width: 0;
    }
    .back-button {
      width: 40px;
      height: 40px;
      border-radius: 12px;
      border: 1px solid rgba(255, 255, 255, 0.32);
      background: rgba(255, 255, 255, 0.15);
      color: #fff;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      text-decoration: none;
      transition: all 0.22s ease;
      flex-shrink: 0;
    }
    .back-button:hover {
      background: rgba(255, 255, 255, 0.24);
      transform: translateY(-1px);
    }
    .header-icon-box {
      width: 50px;
      height: 50px;
      border-radius: 13px;
      border: 1px solid rgba(255, 255, 255, 0.72);
      background: #ffffff;
      box-shadow: 0 8px 18px rgba(15, 23, 42, 0.18);
      padding: 8px;
      flex-shrink: 0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }
    .header-logo {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }
    .header-text-box {
      min-width: 0;
      color: #fff;
    }
    .header-title-text {
      margin: 0;
      font-size: 1.3rem;
      line-height: 1.18;
      font-weight: 700;
      letter-spacing: 0.15px;
      color: #fff;
    }
    .header-subtitle-text {
      margin: 0.24rem 0 0 0;
      font-size: 0.84rem;
      color: rgba(255, 255, 255, 0.94);
      line-height: 1.45;
      max-width: 520px;
    }
    .header-actions {
      display: flex;
      align-items: center;
      gap: 0.55rem;
      min-width: 0;
    }
    .info-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.42rem;
      padding: 0.5rem 0.8rem;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.9);
      color: #065f46;
      font-size: 0.76rem;
      font-weight: 700;
      white-space: nowrap;
      box-shadow: 0 8px 20px rgba(6, 95, 70, 0.16);
    }
    .form-container {
      display: flex;
      flex-direction: column;
      gap: 0.9rem;
      min-width: 0;
    }
    .form-card {
      background: #fff;
      border: 1px solid #d1d5db;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 14px 30px rgba(15, 23, 42, 0.06);
    }
    .card-header {
      background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
      border-bottom: 1px solid #e5e7eb;
      padding: 0.9rem 1rem;
      display: flex;
      align-items: center;
      gap: 0.7rem;
    }
    .card-header-icon {
      width: 40px;
      height: 40px;
      border-radius: 11px;
      flex-shrink: 0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-size: 0.92rem;
      background: linear-gradient(130deg, var(--mz-primary) 0%, var(--mz-primary-soft) 100%);
    }
    .card-header-text {
      min-width: 0;
    }
    .card-title {
      margin: 0;
      font-size: 1.03rem;
      font-weight: 700;
      color: #111827;
      line-height: 1.25;
    }
    .card-subtitle {
      margin: 0.2rem 0 0 0;
      font-size: 0.8rem;
      line-height: 1.4;
      color: #6b7280;
    }
    .card-body {
      padding: 0.96rem 1rem;
    }
    .form-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.72rem;
    }
    .form-group {
      display: flex;
      flex-direction: column;
      gap: 0.4rem;
      min-width: 0;
    }
    .form-group-full {
      grid-column: span 2;
    }
    .form-group-double {
      grid-column: span 1;
    }
    .form-label {
      margin: 0;
      font-size: 0.8rem;
      font-weight: 700;
      color: #1f2937;
      display: inline-flex;
      align-items: center;
      gap: 0.24rem;
      line-height: 1.35;
    }
    .required {
      color: #dc2626;
    }
    .form-input,
    .form-select,
    .form-textarea {
      width: 100%;
      min-width: 0;
      border-radius: 10px;
      border: 1px solid #d1d5db;
      background: #fff;
      color: #111827;
      font-size: 0.88rem;
      line-height: 1.45;
      padding: 0.58rem 0.7rem;
      transition: border-color 0.2s ease, box-shadow 0.2s ease;
    }
    .form-input:focus,
    .form-select:focus,
    .form-textarea:focus {
      outline: none;
      border-color: var(--mz-primary);
      box-shadow: 0 0 0 3px rgba(34, 197, 94, 0.14);
    }
    .form-textarea {
      min-height: 94px;
      resize: vertical;
    }
    .input-readonly {
      background: #f3f4f6;
      cursor: not-allowed;
    }
    .form-hint {
      margin: 0;
      font-size: 0.72rem;
      line-height: 1.46;
      color: #6b7280;
    }
    .input-with-prefix {
      position: relative;
      display: flex;
      align-items: center;
      min-width: 0;
    }
    .input-prefix {
      position: absolute;
      left: 0.74rem;
      font-size: 0.82rem;
      font-weight: 700;
      color: #64748b;
      pointer-events: none;
      z-index: 1;
    }
    .input-with-prefix-field {
      padding-left: 2.1rem;
    }
    .muzakki-section {
      padding: 0.78rem;
      margin-bottom: 0.62rem;
      border-radius: 13px;
      border: 1px solid #e5e7eb;
      background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
      min-width: 0;
    }
    .muzakki-section-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin-bottom: 0.7rem;
    }
    .muzakki-section-title {
      margin: 0;
      font-size: 0.9rem;
      font-weight: 700;
      color: var(--mz-primary);
      display: inline-flex;
      align-items: center;
      gap: 0.42rem;
    }
    .btn-remove-muzakki {
      border: 0;
      border-radius: 9px;
      background: #fee2e2;
      color: #b91c1c;
      font-size: 0.74rem;
      font-weight: 700;
      display: inline-flex;
      align-items: center;
      gap: 0.36rem;
      padding: 0.42rem 0.62rem;
      cursor: pointer;
      transition: transform 0.2s ease, background 0.2s ease, color 0.2s ease;
    }
    .btn-remove-muzakki:hover {
      background: #ef4444;
      color: #fff;
      transform: translateY(-1px);
    }
    .btn-add-muzakki {
      margin-top: 0.5rem;
      border: 0;
      border-radius: 10px;
      padding: 0.58rem 0.9rem;
      font-size: 0.8rem;
      font-weight: 700;
      color: #fff;
      background: linear-gradient(130deg, #15803d 0%, #16a34a 100%);
      box-shadow: 0 10px 18px rgba(22, 163, 74, 0.24);
      display: inline-flex;
      align-items: center;
      gap: 0.42rem;
      cursor: pointer;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .btn-add-muzakki:hover {
      transform: translateY(-1px);
      box-shadow: 0 12px 22px rgba(22, 163, 74, 0.3);
    }
    .preview-container {
      margin-top: 0.2rem;
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.62rem;
      min-width: 0;
    }
    .preview-box {
      border-radius: 12px;
      border: 1px solid;
      padding: 0.62rem;
      display: flex;
      align-items: center;
      gap: 0.56rem;
      min-width: 0;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .preview-box:hover {
      transform: translateY(-1px);
      box-shadow: 0 8px 16px rgba(15, 23, 42, 0.08);
    }
    .preview-box-blue {
      border-color: #bfdbfe;
      background: linear-gradient(145deg, #eff6ff 0%, #dbeafe 100%);
    }
    .preview-box-green {
      border-color: #bbf7d0;
      background: linear-gradient(145deg, #f0fdf4 0%, #dcfce7 100%);
    }
    .preview-icon {
      width: 38px;
      height: 38px;
      border-radius: 10px;
      flex-shrink: 0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-size: 0.86rem;
    }
    .preview-box-blue .preview-icon {
      background: linear-gradient(130deg, #2563eb 0%, #3b82f6 100%);
    }
    .preview-box-green .preview-icon {
      background: linear-gradient(130deg, #059669 0%, #10b981 100%);
    }
    .preview-content {
      min-width: 0;
    }
    .preview-label {
      font-size: 0.7rem;
      font-weight: 700;
      margin: 0;
    }
    .preview-box-blue .preview-label {
      color: #1e40af;
    }
    .preview-box-green .preview-label {
      color: #065f46;
    }
    .preview-value {
      margin-top: 0.2rem;
      font-size: 0.98rem;
      line-height: 1.2;
      font-weight: 700;
      overflow-wrap: anywhere;
    }
    .preview-box-blue .preview-value {
      color: #1e3a8a;
    }
    .preview-box-green .preview-value {
      color: #064e3b;
    }
    .form-actions {
      padding: 0.26rem 0 0;
      display: flex;
      justify-content: flex-end;
      align-items: center;
      gap: 0.56rem;
      flex-wrap: wrap;
    }
    .btn-primary,
    .btn-secondary {
      border-radius: 10px;
      padding: 0.58rem 0.92rem;
      font-size: 0.82rem;
      font-weight: 700;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.42rem;
      text-decoration: none;
      transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
      cursor: pointer;
      border: 0;
    }
    .btn-primary {
      color: #fff;
      background: linear-gradient(130deg, #15803d 0%, #16a34a 100%);
      box-shadow: 0 10px 20px rgba(22, 163, 74, 0.24);
    }
    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 12px 22px rgba(22, 163, 74, 0.32);
    }
    .btn-secondary {
      border: 1px solid #d1d5db;
      background: #f8fafc;
      color: #1f2937;
    }
    .btn-secondary:hover {
      background: #eef2f7;
      transform: translateY(-1px);
    }
    .info-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.72rem;
      min-width: 0;
    }
    .info-card {
      border-radius: 14px;
      border: 1px solid #d1d5db;
      background: #fff;
      box-shadow: 0 12px 24px rgba(15, 23, 42, 0.05);
      padding: 0.76rem;
      display: flex;
      align-items: flex-start;
      gap: 0.58rem;
      min-width: 0;
    }
    .info-card-icon {
      width: 34px;
      height: 34px;
      border-radius: 9px;
      flex-shrink: 0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-size: 0.8rem;
    }
    .info-card-green .info-card-icon {
      background: linear-gradient(130deg, #059669 0%, #10b981 100%);
    }
    .info-card-blue .info-card-icon {
      background: linear-gradient(130deg, #2563eb 0%, #3b82f6 100%);
    }
    .info-card-content {
      min-width: 0;
    }
    .info-card-title {
      margin: 0;
      font-size: 0.9rem;
      line-height: 1.3;
      font-weight: 700;
      color: #111827;
    }
    .info-card-list {
      list-style: none;
      margin: 0.46rem 0 0 0;
      padding: 0;
      display: grid;
      gap: 0.3rem;
    }
    .info-card-list li {
      position: relative;
      padding-left: 0.8rem;
      font-size: 0.74rem;
      line-height: 1.45;
      color: #64748b;
      overflow-wrap: anywhere;
    }
    .info-card-list li::before {
      content: "";
      width: 5px;
      height: 5px;
      border-radius: 9999px;
      position: absolute;
      left: 0;
      top: 0.42rem;
      background: #22c55e;
    }
    .mr-1 {
      margin-right: 4px;
    }
    @media (max-width: 1024px) {
      .form-grid {
        grid-template-columns: 1fr;
      }
      .form-group-full,
      .form-group-double {
        grid-column: span 1;
      }
    }
    @media (max-width: 820px) {
      .page-wrapper {
        padding: 0.5rem 0.3rem 1.05rem;
      }
      .header-content-wrapper {
        flex-direction: column;
        align-items: flex-start;
      }
      .header-actions {
        width: 100%;
      }
      .info-badge {
        width: 100%;
        justify-content: center;
      }
      .preview-container,
      .info-grid {
        grid-template-columns: 1fr;
      }
      .form-actions {
        flex-direction: column-reverse;
        align-items: stretch;
      }
      .btn-primary,
      .btn-secondary {
        width: 100%;
      }
    }
    @media (max-width: 560px) {
      .page-wrapper {
        padding: 0.42rem 0.24rem 0.94rem;
        gap: 0.72rem;
      }
      .page-wrapper .page-header {
        border-radius: 15px;
        padding: 0.84rem 0.76rem;
      }
      .header-left-section {
        gap: 0.58rem;
      }
      .back-button {
        width: 36px;
        height: 36px;
      }
      .header-icon-box {
        width: 42px;
        height: 42px;
        border-radius: 10px;
      }
      .header-title-text {
        font-size: 1.08rem;
      }
      .header-subtitle-text {
        font-size: 0.76rem;
      }
      .card-header,
      .card-body {
        padding: 0.7rem;
      }
      .card-header-icon {
        width: 35px;
        height: 35px;
      }
      .card-title {
        font-size: 0.95rem;
      }
      .card-subtitle {
        font-size: 0.74rem;
      }
      .muzakki-section {
        padding: 0.64rem;
      }
      .info-card {
        padding: 0.66rem;
      }
    }
    @media (max-width: 400px) {
      .page-wrapper {
        padding: 0.36rem 0.2rem 0.86rem;
      }
      .page-wrapper .page-header {
        padding: 0.72rem 0.64rem;
      }
      .header-title-text {
        font-size: 1rem;
      }
      .header-subtitle-text {
        font-size: 0.72rem;
      }
    }
    @media (hover: none) and (pointer: coarse) {
      .btn-primary:hover,
      .btn-secondary:hover,
      .btn-add-muzakki:hover,
      .btn-remove-muzakki:hover,
      .preview-box:hover,
      .back-button:hover {
        transform: none;
      }
    }
    html.dark-mode .form-card,
    html.dark-mode .info-card {
      background: #0f172a;
      border-color: #1f2937;
      box-shadow: none;
    }
    html.dark-mode .card-header {
      background: #111827;
      border-bottom-color: #1f2937;
    }
    html.dark-mode .card-title,
    html.dark-mode .form-label,
    html.dark-mode .info-card-title {
      color: #f8fafc;
    }
    html.dark-mode .card-subtitle,
    html.dark-mode .form-hint,
    html.dark-mode .info-card-list li {
      color: #94a3b8;
    }
    html.dark-mode .form-input,
    html.dark-mode .form-select,
    html.dark-mode .form-textarea {
      background: #0b1220;
      color: #e2e8f0;
      border-color: #334155;
    }
    html.dark-mode .form-input::placeholder,
    html.dark-mode .form-textarea::placeholder {
      color: #64748b;
    }
    html.dark-mode .input-prefix {
      color: #94a3b8;
    }
    html.dark-mode .btn-secondary {
      background: #1f2937;
      color: #e5e7eb;
      border-color: #374151;
    }
    html.dark-mode .btn-secondary:hover {
      background: #374151;
    }
    html.dark-mode .muzakki-section {
      background: #111827;
      border-color: #1f2937;
    }
    html.dark-mode .info-badge {
      background: rgba(2, 6, 23, 0.88);
      color: #86efac;
    }
    html.dark-mode .header-icon-box {
      background: #ffffff;
      border-color: #ffffff;
      box-shadow: 0 8px 18px rgba(2, 6, 23, 0.45);
    }
    html.dark-mode .preview-box-blue {
      border-color: #1d4ed8;
      background: linear-gradient(145deg, rgba(30, 64, 175, 0.35) 0%, rgba(30, 58, 138, 0.26) 100%);
    }
    html.dark-mode .preview-box-green {
      border-color: #047857;
      background: linear-gradient(145deg, rgba(5, 150, 105, 0.34) 0%, rgba(6, 95, 70, 0.22) 100%);
    }
    html.dark-mode .preview-box-blue .preview-label,
    html.dark-mode .preview-box-blue .preview-value {
      color: #bfdbfe;
    }
    html.dark-mode .preview-box-green .preview-label,
    html.dark-mode .preview-box-green .preview-value {
      color: #bbf7d0;
    }
  </style>


  <script>
    // Global variable untuk tracking jumlah muzakki
    let muzakkiCount = 1;

    // Function untuk menghitung dan mengupdate jumlah jiwa otomatis (REAL-TIME)
    function updateJumlahJiwa() {
      const muzakkiSections = document.querySelectorAll(".muzakki-section");
      // Hitung total section muzakki yang ada (tidak peduli isi nama)
      let totalJiwa = muzakkiSections.length;

      // Update field jumlah_jiwa
      const jumlahJiwaInput = document.getElementById("jumlah_jiwa");
      jumlahJiwaInput.value = totalJiwa;

      // Update visual indicator
      if (totalJiwa > 0) {
        jumlahJiwaInput.classList.remove("input-readonly");
        jumlahJiwaInput.classList.add("bg-green-50", "border-green-200");
      } else {
        jumlahJiwaInput.classList.add("input-readonly");
        jumlahJiwaInput.classList.remove("bg-green-50", "border-green-200");
      }

      // Trigger perhitungan zakat
      calculateZakat();

      // Debug log
      console.log(`Total muzakki sections: ${totalJiwa}`);
    }

    // Enhanced calculate zakat function
    function calculateZakat() {
      const jiwaEl = document.getElementById("jumlah_jiwa");
      const masterZakatSelect = document.getElementById("master_zakat_id");

      // Preview elements
      const previewJumlahUang = document.getElementById("preview_jumlah_uang");
      const previewJumlahBerasKg = document.getElementById("preview_jumlah_beras_kg");
      const inputJumlahUang = document.getElementById("jumlah_uang");
      const inputJumlahBerasKg = document.getElementById("jumlah_beras_kg");

      if (!jiwaEl || !masterZakatSelect) {
        return;
      }

      const jiwa = parseInt(jiwaEl.value) || 0;
      const selectedValue = masterZakatSelect.value;

      if (jiwa > 0 && selectedValue) {
        const selectedOption = masterZakatSelect.options[masterZakatSelect.selectedIndex];
        const harga = parseFloat(selectedOption.getAttribute('data-harga')) || 0;
        const kg = parseFloat(selectedOption.getAttribute('data-kg')) || 0;

        if (kg > 0) {
          // Jenis beras
          const totalKg = jiwa * kg;
          const totalUang = jiwa * harga;

          // Update preview dan hidden inputs
          if (previewJumlahUang) {
            previewJumlahUang.textContent = 'Rp ' + Math.floor(totalUang).toLocaleString("id-ID");
          }
          if (previewJumlahBerasKg) {
            previewJumlahBerasKg.textContent = totalKg.toFixed(1) + ' kg';
          }
          if (inputJumlahUang) {
            inputJumlahUang.value = Math.floor(totalUang);
          }
          if (inputJumlahBerasKg) {
            inputJumlahBerasKg.value = totalKg.toFixed(1);
          }
        } else {
          // Jenis uang
          const totalUang = jiwa * harga;

          // Update preview dan hidden inputs
          if (previewJumlahUang) {
            previewJumlahUang.textContent = 'Rp ' + Math.floor(totalUang).toLocaleString("id-ID");
          }
          if (previewJumlahBerasKg) {
            previewJumlahBerasKg.textContent = '0 kg';
          }
          if (inputJumlahUang) {
            inputJumlahUang.value = Math.floor(totalUang);
          }
          if (inputJumlahBerasKg) {
            inputJumlahBerasKg.value = 0;
          }
        }
      } else {
        // Reset semua ke default
        if (previewJumlahUang) {
          previewJumlahUang.textContent = 'Rp 0';
        }
        if (previewJumlahBerasKg) {
          previewJumlahBerasKg.textContent = '0 kg';
        }
        if (inputJumlahUang) {
          inputJumlahUang.value = 0;
        }
        if (inputJumlahBerasKg) {
          inputJumlahBerasKg.value = 0;
        }
      }
    }

    // Function untuk menambah section muzakki baru
    function addMuzakkiSection() {
      const container = document.getElementById("muzakkiContainer");
      const newIndex = muzakkiCount;

      // HTML untuk section muzakki baru
      const newSection = `
      <div class="muzakki-section" data-index="${newIndex}">
        <div class="muzakki-section-header">
          <h4 class="muzakki-section-title">
            <i class="fas fa-user-circle"></i>Muzakki #${newIndex + 1}
          </h4>
          <button
            type="button"
            class="btn-remove-muzakki"
            onclick="removeMuzakkiSection(${newIndex})"
          >
            <i class="fas fa-trash"></i>Hapus
          </button>
        </div>
        
        <div class="form-grid">
          <!-- Nama Muzakki -->
          <div class="form-group form-group-full">
            <label
              for="nama_${newIndex}"
              class="form-label"
            >
              <i class="fas fa-user mr-1"></i>Nama Lengkap Muzakki <span class="required">*</span>
            </label>
            <input
              type="text"
              id="nama_${newIndex}"
              name="muzakki[${newIndex}][nama]"
              required
              class="form-input"
              placeholder="Masukkan nama lengkap muzakki"
            />
          </div>

          <!-- Bin/Binti -->
          <div class="form-group">
            <label
              for="bin_binti_${newIndex}"
              class="form-label"
            >
              <i class="fas fa-venus-mars mr-1"></i>Bin/Binti
            </label>
            <select
              id="bin_binti_${newIndex}"
              name="muzakki[${newIndex}][bin_binti]"
              class="form-select"
            >
              <option value="">Pilih</option>
              <option value="bin">Bin (Anak Laki-laki)</option>
              <option value="binti">Binti (Anak Perempuan)</option>
            </select>
            <p class="form-hint">
              Bin untuk laki-laki, Binti untuk perempuan
            </p>
          </div>

          <!-- Nama Orang Tua -->
          <div class="form-group form-group-double">
            <label
              for="nama_orang_tua_${newIndex}"
              class="form-label"
            >
              <i class="fas fa-users mr-1"></i>Nama Orang Tua
            </label>
            <input
              type="text"
              id="nama_orang_tua_${newIndex}"
              name="muzakki[${newIndex}][nama_orang_tua]"
              class="form-input"
              placeholder="Masukkan nama ayah atau ibu sesuai bin/binti"
            />
            <p class="form-hint">
              Nama ayah jika "bin", nama ibu jika "binti"
            </p>
          </div>
        </div>
      </div>
    `;

      // Tambahkan section baru ke container
      container.insertAdjacentHTML("beforeend", newSection);

      // Update counter
      muzakkiCount++;

      // Update visibility tombol tambah (maksimal 5 muzakki)
      updateAddButtonVisibility();

      // Update jumlah jiwa LANGSUNG setelah menambah section
      updateJumlahJiwa();

      // Scroll ke section yang baru ditambahkan
      const newSectionElement = container.lastElementChild;
      newSectionElement.scrollIntoView({ behavior: "smooth", block: "center" });
    }

    // Function untuk menghapus section muzakki dengan SweetAlert2
    function removeMuzakkiSection(index) {
      // Cek apakah SweetAlert2 tersedia
      if (typeof Swal !== 'undefined') {
        Swal.fire({
          title: 'Hapus Muzakki?',
          text: 'Apakah Anda yakin ingin menghapus data muzakki ini?',
          icon: 'warning',
          showCancelButton: true,
          confirmButtonColor: '#ef4444',
          cancelButtonColor: '#6b7280',
          confirmButtonText: 'Ya, Hapus',
          cancelButtonText: 'Batal',
          reverseButtons: true
        }).then((result) => {
          if (result.isConfirmed) {
            const section = document.querySelector(`[data-index="${index}"]`);
            if (section) {
              section.remove();
              muzakkiCount--;
              updateAddButtonVisibility();
              updateMuzakkiNumbers();
              updateJumlahJiwa(); // Update jumlah jiwa setelah hapus

              // Tampilkan notifikasi sukses
              Swal.fire({
                title: 'Terhapus!',
                text: 'Data muzakki berhasil dihapus.',
                icon: 'success',
                timer: 1500,
                showConfirmButton: false
              });
            }
          }
        });
      } else {
        // Fallback ke confirm default jika SweetAlert2 tidak tersedia
        if (confirm("Apakah Anda yakin ingin menghapus data muzakki ini?")) {
          const section = document.querySelector(`[data-index="${index}"]`);
          if (section) {
            section.remove();
            muzakkiCount--;
            updateAddButtonVisibility();
            updateMuzakkiNumbers();
            updateJumlahJiwa();
          }
        }
      }
    }

    // Function untuk update nomor muzakki setelah penghapusan
    function updateMuzakkiNumbers() {
      const sections = document.querySelectorAll(".muzakki-section");
      sections.forEach((section, index) => {
        const title = section.querySelector(".muzakki-section-title");
        if (title) {
          title.innerHTML = `<i class="fas fa-user-circle"></i>Muzakki #${index + 1
            }`;
        }
      });
    }

    // Function untuk mengatur visibility tombol tambah
    function updateAddButtonVisibility() {
      const addBtn = document.getElementById("addMuzakkiBtn");
      const sections = document.querySelectorAll(".muzakki-section");

      if (sections.length >= 5) {
        addBtn.style.display = "none";
      } else {
        addBtn.style.display = "inline-flex";
      }
    }

    // Rupiah formatting functions
    function formatToRupiah(number) {
      if (!number || number === 0) return "0";
      return new Intl.NumberFormat("id-ID").format(number);
    }

    function parseFromRupiah(rupiahString) {
      if (!rupiahString) return 0;
      // Remove all non-numeric characters
      const cleanNumber = rupiahString.replace(/[^\d]/g, "");
      return parseInt(cleanNumber) || 0;
    }

    // Handle rupiah input in real-time
    function handleRupiahInput(input) {
      // Get current cursor position
      const cursorPosition = input.selectionStart;
      const oldValue = input.value;

      // Extract numbers only
      const numericValue = input.value.replace(/[^\d]/g, "");
      const numberValue = parseInt(numericValue) || 0;

      // Format to rupiah
      const formattedValue = formatToRupiah(numberValue);

      // Update display input
      input.value = formattedValue;

      // Update hidden input
      document.getElementById("jumlah_bayar").value = numberValue;

      // Restore cursor position (approximate)
      const newLength = formattedValue.length;
      const oldLength = oldValue.length;
      const newCursorPosition = cursorPosition + (newLength - oldLength);

      setTimeout(() => {
        input.setSelectionRange(newCursorPosition, newCursorPosition);
      }, 0);
    }

    // Prepare input when focused
    function prepareRupiahInput(input) {
      const hiddenValue = document.getElementById("jumlah_bayar").value;
      if (hiddenValue && hiddenValue !== "0") {
        // Show raw number for easier editing
        input.value = hiddenValue;
        // Select all for easy replacement
        setTimeout(() => input.select(), 50);
      }
    }

    // Finalize input when focus is lost
    function finalizeRupiahInput(input) {
      const numericValue = parseFromRupiah(input.value);
      const formattedValue = formatToRupiah(numericValue);

      // Update both inputs
      input.value = formattedValue;
      document.getElementById("jumlah_bayar").value = numericValue;
    }

    // Add event listeners when document is ready
    document.addEventListener("DOMContentLoaded", function () {
      console.log("=== DOM Content Loaded ===");
      
      const displayInput = document.getElementById("jumlah_bayar_display");

      // Prevent non-numeric input
      displayInput.addEventListener("keypress", function (e) {
        // Allow: backspace, delete, tab, escape, enter, and numbers
        if (
          !/[\d]/.test(e.key) &&
          ![
            "Backspace",
            "Delete",
            "Tab",
            "ArrowLeft",
            "ArrowRight",
            "Enter",
          ].includes(e.key)
        ) {
          e.preventDefault();
        }
      });

      // Handle paste events
      displayInput.addEventListener("paste", function (e) {
        e.preventDefault();
        const pastedText = (e.clipboardData || window.clipboardData).getData(
          "text"
        );
        const numericValue = pastedText.replace(/[^\d]/g, "");
        if (numericValue) {
          this.value = numericValue;
          handleRupiahInput(this);
        }
      });

      // Initialize jumlah jiwa dengan menghitung muzakki yang ada
      updateJumlahJiwa();

      // Add event listener untuk jenis zakat
      const masterZakatSelect = document.getElementById("master_zakat_id");
      if (masterZakatSelect) {
        console.log("Found master_zakat_id element");
        
        // Remove any existing onchange
        masterZakatSelect.onchange = null;
        
        // Add new event listener
        masterZakatSelect.addEventListener("change", function() {
          console.log("Event: Jenis zakat changed to:", this.value);
          calculateZakat();
        });
        
        console.log("Event listener added successfully");
        
        // Trigger initial calculation if already selected
        if (masterZakatSelect.value) {
          console.log("Initial value exists:", masterZakatSelect.value);
          setTimeout(() => {
            calculateZakat();
          }, 300);
        }
      } else {
        console.error("Element master_zakat_id not found!");
      }
    });

    // Form validation
    document
      .getElementById("muzakkiForm")
      .addEventListener("submit", function (e) {
        const jiwa = parseInt(document.getElementById("jumlah_jiwa").value);
        const tanggal = document.getElementById("tanggal").value;
        const masterZakatId = document.getElementById("master_zakat_id").value;
        const jumlahBayar = parseInt(
          document.getElementById("jumlah_bayar").value
        );

        // Validasi basic fields
        if (jiwa <= 0) {
          e.preventDefault();
          if (typeof Swal !== 'undefined') {
            Swal.fire({
              icon: 'error',
              title: 'Validasi Gagal',
              text: 'Jumlah jiwa harus lebih dari 0. Tambahkan minimal 1 muzakki.',
              confirmButtonColor: '#1EAF2F'
            });
          } else {
            alert("Jumlah jiwa harus lebih dari 0");
          }
          return false;
        }

        if (!masterZakatId) {
          e.preventDefault();
          if (typeof Swal !== 'undefined') {
            Swal.fire({
              icon: 'error',
              title: 'Validasi Gagal',
              text: 'Pilih jenis zakat terlebih dahulu',
              confirmButtonColor: '#1EAF2F'
            });
          } else {
            alert("Pilih jenis zakat terlebih dahulu");
          }
          return false;
        }

        if (!tanggal) {
          e.preventDefault();
          if (typeof Swal !== 'undefined') {
            Swal.fire({
              icon: 'error',
              title: 'Validasi Gagal',
              text: 'Tanggal zakat wajib diisi',
              confirmButtonColor: '#1EAF2F'
            });
          } else {
            alert("Tanggal zakat wajib diisi");
          }
          return false;
        }

        if (jumlahBayar <= 0 || isNaN(jumlahBayar)) {
          e.preventDefault();
          if (typeof Swal !== 'undefined') {
            Swal.fire({
              icon: 'error',
              title: 'Validasi Gagal',
              text: 'Jumlah bayar harus lebih dari 0',
              confirmButtonColor: '#1EAF2F'
            });
          } else {
            alert("Jumlah bayar harus lebih dari 0");
          }
          return false;
        }

        // Validasi semua section muzakki
        const muzakkiSections = document.querySelectorAll(".muzakki-section");
        let hasEmptyName = false;
        let emptyNameIndex = -1;

        muzakkiSections.forEach((section, index) => {
          const namaInput = section.querySelector(
            `input[name^="muzakki"][name$="[nama]"]`
          );
          if (!namaInput.value.trim()) {
            hasEmptyName = true;
            emptyNameIndex = index + 1;
          }
        });

        if (hasEmptyName) {
          e.preventDefault();
          if (typeof Swal !== 'undefined') {
            Swal.fire({
              icon: 'error',
              title: 'Validasi Gagal',
              text: `Nama muzakki #${emptyNameIndex} tidak boleh kosong`,
              confirmButtonColor: '#1EAF2F'
            });
          } else {
            alert(`Nama muzakki #${emptyNameIndex} tidak boleh kosong`);
          }
          return false;
        }

        // Konfirmasi final dengan SweetAlert2
        e.preventDefault();
        const muzakkiCount = muzakkiSections.length;
        const masterZakatSelect = document.getElementById("master_zakat_id");
        const jenisZakatText = masterZakatSelect.options[masterZakatSelect.selectedIndex].text;

        if (typeof Swal !== 'undefined') {
          Swal.fire({
            title: 'Konfirmasi Data',
            html: `
            <div style="text-align: left; padding: 10px;">
              <p style="margin-bottom: 8px;"><strong>Apakah data yang dimasukkan sudah benar?</strong></p>
              <ul style="list-style: none; padding-left: 0;">
                <li style="margin-bottom: 6px;">Tanggal Zakat: <strong>${new Date(`${tanggal}T00:00:00`).toLocaleDateString("id-ID")}</strong></li>
                <li style="margin-bottom: 6px;">Jumlah Muzakki: <strong>${muzakkiCount} orang</strong></li>
                <li style="margin-bottom: 6px;">Jumlah Jiwa: <strong>${jiwa} jiwa</strong></li>
                <li style="margin-bottom: 6px;">Jenis Zakat: <strong>${jenisZakatText}</strong></li>
                <li style="margin-bottom: 6px;">Jumlah Bayar: <strong>Rp ${jumlahBayar.toLocaleString("id-ID")}</strong></li>
              </ul>
            </div>
          `,
            icon: 'question',
            showCancelButton: true,
            confirmButtonColor: '#1EAF2F',
            cancelButtonColor: '#6b7280',
            confirmButtonText: 'Ya, Simpan',
            cancelButtonText: 'Periksa Lagi',
            reverseButtons: true
          }).then((result) => {
            if (result.isConfirmed) {
              // Submit form
              document.getElementById("muzakkiForm").submit();
            }
          });
        } else {
          // Fallback ke confirm default
          const confirmMessage =
            `Apakah data yang dimasukkan sudah benar?\n\n` +
            `- Tanggal Zakat: ${new Date(`${tanggal}T00:00:00`).toLocaleDateString("id-ID")}\n` +
            `- Jumlah Muzakki: ${muzakkiCount} orang\n` +
            `- Jumlah Jiwa: ${jiwa} jiwa\n` +
            `- Jenis Zakat: ${jenisZakatText}\n` +
            `- Jumlah Bayar: Rp ${jumlahBayar.toLocaleString("id-ID")}`;

          if (confirm(confirmMessage)) {
            document.getElementById("muzakkiForm").submit();
          }
        }

        return false;
      });
  </script>

  <%- include('../layouts/footer') %>

