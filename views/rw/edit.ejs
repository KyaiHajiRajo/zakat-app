<%- include('../layouts/header', { title: 'Edit RW - Zakat Fitrah App', bodyClass: 'bg-gray-50 min-h-screen', currentPage: 'rt' }) %>

<style>
  .rw-edit-page {
    position: relative;
    max-width: 1180px;
    margin: 0 auto;
    padding: 0.42rem 0.14rem 0.92rem;
  }

  .rw-edit-shell {
    position: relative;
    z-index: 2;
    display: grid;
    gap: 0.82rem;
  }

  .rw-edit-orb {
    position: absolute;
    border-radius: 9999px;
    filter: blur(10px);
    pointer-events: none;
    z-index: 1;
  }

  .rw-edit-orb-a {
    width: 210px;
    height: 210px;
    top: 18px;
    right: -68px;
    background: radial-gradient(circle, rgba(34, 197, 94, 0.2) 0%, rgba(34, 197, 94, 0) 72%);
  }

  .rw-edit-orb-b {
    width: 170px;
    height: 170px;
    bottom: 70px;
    left: -52px;
    background: radial-gradient(circle, rgba(59, 130, 246, 0.16) 0%, rgba(59, 130, 246, 0) 72%);
  }

  .rw-edit-hero {
    background: linear-gradient(125deg, #0f766e 0%, #16a34a 58%, #22c55e 100%);
    border-radius: 17px;
    color: #fff;
    padding: 0.74rem 0.74rem;
    box-shadow: 0 16px 34px rgba(22, 163, 74, 0.24);
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.74rem;
    position: relative;
    overflow: hidden;
  }

  .rw-edit-hero::after {
    content: "";
    position: absolute;
    width: 250px;
    height: 250px;
    border-radius: 9999px;
    top: -154px;
    right: -74px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.24) 0%, rgba(255, 255, 255, 0) 70%);
  }

  .rw-edit-hero-main {
    position: relative;
    z-index: 2;
    min-width: 0;
    display: flex;
    align-items: center;
    gap: 0.65rem;
  }

  .rw-edit-back {
    width: 38px;
    height: 38px;
    border-radius: 10px;
    border: 1px solid rgba(255, 255, 255, 0.3);
    background: rgba(255, 255, 255, 0.14);
    color: #fff;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s ease, transform 0.2s ease;
    flex-shrink: 0;
  }

  .rw-edit-back:hover {
    background: rgba(255, 255, 255, 0.22);
    transform: translateY(-1px);
  }

  .rw-edit-title {
    margin: 0;
    font-size: 1.22rem;
    line-height: 1.2;
    font-weight: 700;
  }

  .rw-edit-subtitle {
    margin: 0.2rem 0 0;
    font-size: 0.78rem;
    color: rgba(255, 255, 255, 0.92);
  }

  .rw-edit-chip {
    position: relative;
    z-index: 2;
    border-radius: 9999px;
    padding: 0.5rem 0.78rem;
    background: rgba(255, 255, 255, 0.9);
    color: #065f46;
    font-size: 0.73rem;
    font-weight: 700;
    display: inline-flex;
    align-items: center;
    gap: 0.36rem;
    white-space: nowrap;
  }

  .rw-edit-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    gap: 0.84rem;
    align-items: start;
  }

  .rw-edit-card {
    border-radius: 16px;
    background: #fff;
    border: 1px solid #d1d5db;
    box-shadow: 0 14px 30px rgba(15, 23, 42, 0.06);
    overflow: hidden;
  }

  .rw-edit-card-head {
    padding: 0.74rem 0.76rem;
    border-bottom: 1px solid #e5e7eb;
    background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
  }

  .rw-edit-card-title {
    margin: 0;
    font-size: 0.98rem;
    color: #0f172a;
    font-weight: 700;
  }

  .rw-edit-card-desc {
    margin: 0.2rem 0 0;
    font-size: 0.76rem;
    color: #64748b;
    line-height: 1.46;
  }

  .rw-edit-form {
    padding: 0.74rem 0.76rem;
    display: grid;
    gap: 0.72rem;
  }

  .rw-edit-field {
    border: 1px solid #e5e7eb;
    background: #f8fafc;
    border-radius: 12px;
    padding: 0.56rem;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .rw-edit-field:focus-within {
    border-color: rgba(22, 163, 74, 0.4);
    box-shadow: 0 0 0 4px rgba(34, 197, 94, 0.12);
  }

  .rw-edit-label {
    margin: 0 0 0.36rem;
    font-size: 0.78rem;
    font-weight: 700;
    color: #1f2937;
  }

  .rw-edit-required {
    color: #dc2626;
  }

  .rw-edit-input-wrap {
    position: relative;
  }

  .rw-edit-input-wrap i {
    position: absolute;
    left: 0.72rem;
    top: 50%;
    transform: translateY(-50%);
    color: #94a3b8;
    font-size: 0.82rem;
    pointer-events: none;
  }

  .rw-edit-input-wrap-textarea i {
    top: 0.86rem;
    transform: none;
  }

  .rw-edit-control {
    width: 100%;
    border: 1px solid #d1d5db;
    background: #fff;
    border-radius: 9px;
    color: #0f172a;
    font-size: 0.84rem;
    line-height: 1.46;
    padding: 0.56rem 0.62rem 0.56rem 2.02rem;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .rw-edit-control:focus {
    outline: none;
    border-color: #16a34a;
    box-shadow: 0 0 0 3px rgba(34, 197, 94, 0.14);
  }

  .rw-edit-help {
    margin: 0.34rem 0 0;
    font-size: 0.71rem;
    color: #64748b;
    line-height: 1.44;
  }

  .rw-edit-actions {
    margin-top: 0.14rem;
    border-top: 1px solid #e5e7eb;
    padding-top: 0.76rem;
    display: flex;
    justify-content: flex-end;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .rw-edit-btn {
    border: 0;
    border-radius: 9px;
    padding: 0.56rem 0.84rem;
    font-size: 0.78rem;
    font-weight: 700;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.4rem;
    text-decoration: none;
    cursor: pointer;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
  }

  .rw-edit-btn:hover {
    transform: translateY(-1px);
  }

  .rw-edit-btn-secondary {
    background: #e5e7eb;
    color: #1f2937;
  }

  .rw-edit-btn-secondary:hover {
    background: #d1d5db;
  }

  .rw-edit-btn-primary {
    background: linear-gradient(130deg, #15803d 0%, #16a34a 100%);
    color: #fff;
    box-shadow: 0 10px 20px rgba(22, 163, 74, 0.24);
  }

  .rw-edit-btn-primary:hover {
    box-shadow: 0 12px 24px rgba(22, 163, 74, 0.3);
  }

  .rw-edit-side {
    display: grid;
    gap: 0.84rem;
  }

  .rw-edit-side-card {
    border-radius: 16px;
    background: #fff;
    border: 1px solid #d1d5db;
    box-shadow: 0 14px 30px rgba(15, 23, 42, 0.05);
    padding: 0.72rem;
  }

  .rw-edit-side-title {
    margin: 0;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.9rem;
    color: #0f172a;
    font-weight: 700;
  }

  .rw-edit-side-title i {
    width: 27px;
    height: 27px;
    border-radius: 8px;
    background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%);
    color: #1d4ed8;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 0.74rem;
  }

  .rw-edit-summary {
    margin: 0.72rem 0 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0.42rem;
  }

  .rw-edit-summary li {
    border: 1px solid #e5e7eb;
    border-radius: 10px;
    background: #f8fafc;
    padding: 0.5rem 0.56rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.6rem;
  }

  .rw-edit-summary span {
    font-size: 0.68rem;
    text-transform: uppercase;
    letter-spacing: 0.3px;
    color: #64748b;
    font-weight: 700;
  }

  .rw-edit-summary strong {
    font-size: 0.78rem;
    color: #0f172a;
    font-weight: 700;
    text-align: right;
  }

  .rw-edit-side-warning {
    border-color: #fcd34d;
    background: linear-gradient(165deg, #fffbeb 0%, #fef3c7 100%);
  }

  .rw-edit-side-warning .rw-edit-side-title i {
    background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
    color: #b45309;
  }

  .rw-edit-warning-list {
    margin: 0.72rem 0 0;
    padding-left: 1rem;
    display: grid;
    gap: 0.44rem;
    color: #92400e;
    font-size: 0.77rem;
    line-height: 1.48;
  }

  .rw-edit-reveal {
    opacity: 0;
    transform: translateY(10px);
    animation: rwEditReveal 0.52s ease forwards;
    animation-delay: var(--rw-edit-delay, 0ms);
  }

  @keyframes rwEditReveal {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (min-width: 1024px) {
    .rw-edit-grid {
      grid-template-columns: minmax(0, 1fr) 330px;
    }
  }

  @media (max-width: 760px) {
    .rw-edit-page {
      padding: 0.34rem 0.1rem 0.76rem;
    }

    .rw-edit-hero {
      border-radius: 14px;
      padding: 0.62rem;
      flex-direction: column;
      align-items: flex-start;
    }

    .rw-edit-chip {
      white-space: normal;
    }
  }

  @media (max-width: 460px) {
    .rw-edit-page {
      padding: 0.28rem 0.08rem 0.64rem;
    }

    .rw-edit-title {
      font-size: 1.05rem;
    }

    .rw-edit-subtitle {
      font-size: 0.72rem;
    }

    .rw-edit-card-head,
    .rw-edit-form,
    .rw-edit-side-card {
      padding: 0.6rem;
    }

    .rw-edit-actions {
      justify-content: stretch;
    }

    .rw-edit-btn {
      width: 100%;
    }
  }

  html.dark-mode .rw-edit-hero {
    box-shadow: 0 12px 28px rgba(2, 6, 23, 0.45);
  }

  html.dark-mode .rw-edit-chip {
    background: rgba(2, 6, 23, 0.88);
    color: #86efac;
  }

  html.dark-mode .rw-edit-card,
  html.dark-mode .rw-edit-side-card {
    background: #0f172a;
    border-color: #1f2937;
    box-shadow: none;
  }

  html.dark-mode .rw-edit-card-head {
    background: #111827;
    border-bottom-color: #1f2937;
  }

  html.dark-mode .rw-edit-card-title,
  html.dark-mode .rw-edit-label,
  html.dark-mode .rw-edit-side-title,
  html.dark-mode .rw-edit-summary strong {
    color: #f8fafc;
  }

  html.dark-mode .rw-edit-card-desc,
  html.dark-mode .rw-edit-help,
  html.dark-mode .rw-edit-summary span {
    color: #94a3b8;
  }

  html.dark-mode .rw-edit-field,
  html.dark-mode .rw-edit-summary li {
    background: #111827;
    border-color: #1f2937;
  }

  html.dark-mode .rw-edit-control {
    background: #0b1220;
    border-color: #334155;
    color: #e2e8f0;
  }

  html.dark-mode .rw-edit-control::placeholder {
    color: #64748b;
  }

  html.dark-mode .rw-edit-btn-secondary {
    background: #1f2937;
    color: #e2e8f0;
  }

  html.dark-mode .rw-edit-btn-secondary:hover {
    background: #374151;
  }

  html.dark-mode .rw-edit-side-warning {
    border-color: #854d0e;
    background: linear-gradient(165deg, rgba(120, 53, 15, 0.3) 0%, rgba(68, 64, 60, 0.14) 100%);
  }

  html.dark-mode .rw-edit-warning-list {
    color: #fcd34d;
  }
</style>

<section class="rw-edit-page">
  <div class="rw-edit-orb rw-edit-orb-a"></div>
  <div class="rw-edit-orb rw-edit-orb-b"></div>

  <div class="rw-edit-shell">
    <header class="rw-edit-hero rw-edit-reveal" style="--rw-edit-delay: 0ms;">
      <div class="rw-edit-hero-main">
        <a href="/rw" class="rw-edit-back" aria-label="Kembali ke daftar RW">
          <i class="fas fa-arrow-left"></i>
        </a>
        <div>
          <h1 class="rw-edit-title"><i class="fas fa-pen-to-square mr-1"></i>Edit RW</h1>
          <p class="rw-edit-subtitle">Perbarui data RW agar struktur wilayah tetap akurat dan konsisten.</p>
        </div>
      </div>
      <span class="rw-edit-chip"><i class="fas fa-circle-info"></i>Mengubah <strong><%= rw.nomor_rw %></strong></span>
    </header>

    <div class="rw-edit-grid">
      <article class="rw-edit-card rw-edit-reveal" style="--rw-edit-delay: 90ms;">
        <div class="rw-edit-card-head">
          <h2 class="rw-edit-card-title">Formulir Edit RW</h2>
          <p class="rw-edit-card-desc">Periksa kembali semua perubahan sebelum menyimpan data.</p>
        </div>

        <form method="POST" action="/rw/<%= rw.id %>/edit" class="rw-edit-form" id="rw-form">
          <div class="rw-edit-field">
            <label for="nomor_rw" class="rw-edit-label">Nomor RW <span class="rw-edit-required">*</span></label>
            <div class="rw-edit-input-wrap">
              <i class="fas fa-building"></i>
              <input type="text" id="nomor_rw" name="nomor_rw" required value="<%= rw.nomor_rw %>" placeholder="Contoh: RW 01, RW 02, RW 11" class="rw-edit-control" />
            </div>
            <p class="rw-edit-help">Format yang disarankan: RW 01, RW 02, RW 11.</p>
          </div>

          <div class="rw-edit-field">
            <label for="ketua_rw" class="rw-edit-label">Nama Ketua RW <span class="rw-edit-required">*</span></label>
            <div class="rw-edit-input-wrap">
              <i class="fas fa-user"></i>
              <input type="text" id="ketua_rw" name="ketua_rw" required value="<%= rw.ketua_rw %>" placeholder="Nama lengkap ketua RW" class="rw-edit-control" />
            </div>
            <p class="rw-edit-help">Gunakan nama resmi agar data mudah diverifikasi.</p>
          </div>

          <div class="rw-edit-field">
            <label for="keterangan" class="rw-edit-label">Keterangan</label>
            <div class="rw-edit-input-wrap rw-edit-input-wrap-textarea">
              <i class="fas fa-note-sticky"></i>
              <textarea id="keterangan" name="keterangan" rows="4" placeholder="Catatan tambahan atau keterangan RW (opsional)" class="rw-edit-control"><%= rw.keterangan || '' %></textarea>
            </div>
            <p class="rw-edit-help">Opsional, isi jika ada catatan penting terkait RW ini.</p>
          </div>

          <div class="rw-edit-actions">
            <a href="/rw" class="rw-edit-btn rw-edit-btn-secondary">
              <i class="fas fa-xmark"></i>Batal
            </a>
            <button type="submit" class="rw-edit-btn rw-edit-btn-primary">
              <i class="fas fa-floppy-disk"></i>Simpan Perubahan
            </button>
          </div>
        </form>
      </article>

      <aside class="rw-edit-side">
        <div class="rw-edit-side-card rw-edit-reveal" style="--rw-edit-delay: 150ms;">
          <h3 class="rw-edit-side-title"><i class="fas fa-table-list"></i>Ringkasan Data</h3>
          <ul class="rw-edit-summary">
            <li><span>Nomor RW</span><strong><%= rw.nomor_rw || '-' %></strong></li>
            <li><span>Ketua RW</span><strong><%= rw.ketua_rw || '-' %></strong></li>
            <li><span>Keterangan</span><strong><%= rw.keterangan || '-' %></strong></li>
          </ul>
        </div>

        <div class="rw-edit-side-card rw-edit-side-warning rw-edit-reveal" style="--rw-edit-delay: 210ms;">
          <h3 class="rw-edit-side-title"><i class="fas fa-triangle-exclamation"></i>Informasi Penting</h3>
          <ul class="rw-edit-warning-list">
            <li>Perubahan nomor RW dapat mempengaruhi relasi data RT.</li>
            <li>Pastikan nomor RW baru tidak bentrok dengan RW lain.</li>
            <li>Periksa data sebelum menyimpan agar tidak terjadi duplikasi.</li>
          </ul>
        </div>
      </aside>
    </div>
  </div>
</section>

<script>
  const rwForm = document.getElementById("rw-form");
  const nomorRwInput = document.getElementById("nomor_rw");
  const ketuaRwInput = document.getElementById("ketua_rw");

  rwForm.addEventListener("submit", function (e) {
    e.preventDefault();
    const nomorRw = nomorRwInput.value.trim();
    const ketuaRw = ketuaRwInput.value.trim();

    if (!nomorRw || !ketuaRw) {
      Swal.fire({
        icon: "error",
        title: "Error",
        text: "Nomor RW dan Nama Ketua RW harus diisi.",
        confirmButtonText: "OK",
      });
      return false;
    }

    Swal.fire({
      title: "Konfirmasi Simpan",
      text: "Apakah perubahan data RW sudah benar?",
      icon: "question",
      showCancelButton: true,
      confirmButtonColor: "#15803d",
      cancelButtonColor: "#64748b",
      confirmButtonText: "Ya, Simpan",
      cancelButtonText: "Batal",
    }).then((result) => {
      if (result.isConfirmed) {
        this.submit();
      }
    });
  });

  nomorRwInput.addEventListener("input", function (e) {
    let value = e.target.value.toUpperCase();

    if (/^\d+$/.test(value)) {
      const num = parseInt(value, 10);
      e.target.value = num < 10 ? "RW 0" + num : "RW " + num;
    }
  });
</script>

<% if (error) { %>
<script>
  Swal.fire({
    icon: "error",
    title: "Error",
    text: <%- JSON.stringify(error) %>,
    confirmButtonText: "OK",
  });
</script>
<% } %>

<% if (success) { %>
<script>
  Swal.fire({
    icon: "success",
    title: "Berhasil",
    text: <%- JSON.stringify(success) %>,
    confirmButtonText: "OK",
  });
</script>
<% } %>

<%- include('../layouts/footer') %>
